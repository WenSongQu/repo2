package com.supergenius.admin;

import com.baomidou.mybatisplus.core.metadata.IPage;
import com.supergenius.admin.capital.controller.ProjectController;
import com.supergenius.admin.capital.model.VCProjectDO;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.web.WebAppConfiguration;

/**
 * 注意测试时候模块分包测试，不要在这里测试
 */
@SpringBootTest
@WebAppConfiguration
public abstract class BaseProjSupergeniusVcAdminApplicationTests {

    @Autowired
    ProjectController projectController;
    @Test
    void contextLoads() {
//        IPage<VCProjectDO> vcProjectDOIPage = projectController.selectAllProjectByPage(1,3);
//       vcProjectDOIPage.getRecords().forEach(System.out::println);
    }

}
